/**
 * Agent nodes module exports
 * 
 * This file is a placeholder for future agent node extraction.
 * Agent nodes (catalogNode, cartAndCheckoutNode, dealsNode, notificationAgent) 
 * remain in supervisor.ts to avoid circular dependency issues during this refactoring.
 * 
 * They can be extracted to separate files in a future refactoring once
 * all dependencies are properly modularized.
 */

// TODO: Extract agent nodes to separate files:
// - catalog-node.ts
// - cart-checkout-node.ts  
// - deals-node.ts
// - notification-node.ts
